spring:
  application:
    name:
    secret:
      key:
    expiration: 1800000
  jpa:
    hibernate:
      dialect: jakarta.persistence.jdbc.url
      ddl-auto: create
    show-sql: true
  datasource:
    url: jdbc:mysql://localhost:3306/app_restaurant
    username: root
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver